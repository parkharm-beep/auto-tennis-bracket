# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# 작업 산출물 (런타임 생성)
_workspace/
_workspace_prev/
build/

# 결과 파일 (각자 환경에서 생성)
출력/*.xlsx

# 입력 양식 — 회원이 채워넣는 사본은 커밋 안 함
입력/*.xlsx

# OS
.DS_Store
Thumbs.db

# 에디터
.vscode/
.idea/
*.swp
*.swo

# 노드 (StatiCrypt 등)
node_modules/
package-lock.json
.gstack/

# 에이전트 런타임 상태 (web/ 아래에 생기면 gh-pages로 딸려간다)
.omc/
.codex/
.agents/**/__pycache__/

# 회귀 검사 — 입력은 실명이 들어 있어 커밋 안 함(리포가 공개). 기준선 지표(baseline.json)만 커밋
회귀/입력/
회귀/last_run.json
