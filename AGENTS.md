# auto-tennis-bracket

테니스 클럽 주간 대진표 자동 생성 프로젝트.

## 하네스: 테니스 대진표

**목표:** 참가자 명단(이름·성별·구력·정/게·IN/OUT) + 코트 운영시간 → 30분 단위 슬롯×코트 매트릭스의 대진표 엑셀 출력.

**트리거:** "대진표", "대진 짜줘", "테니스 스케줄", "코트 배정", "참가자 명단으로 대진", "입력 양식", "테니스 입력 템플릿" 등 테니스 대진 관련 요청 시 `tennis-bracket-orchestrator` 스킬을 사용하라. 단순 질문(예: "openpyxl 어떻게 쓰지?")은 직접 응답 가능.

**워크플로우:**
- **Phase A (입력 템플릿 생성):** "양식 만들어줘" → `excel-renderer` 단독 호출
- **Phase B (대진표 생성):** 채워진 입력 엑셀 제공 → 4-에이전트 팀(`requirements-parser → bracket-composer ⇄ bracket-reviewer → excel-renderer`)
- **Phase C (주간 입력 초안):** 카카오 일정 URL → browse로 참석/불참/게스트 읽기 → `build_draft.py`로 입력 엑셀 초안(금배=20년·은배=15년·동배=10년) → **초안+검사 보고까지만, 대진표 생성은 사용자가 직접** (상세는 CLAUDE.md·오케스트레이터 스킬)

**중간 산출물:** `_workspace/01_parsed.json`, `02_bracket.json`, `03_review.json` — 재실행/부분 수정 시 활용. 새 실행 시 기존 `_workspace/`는 `_workspace_prev/`로 이동.

**규칙 요약 (전체 가이드는 오케스트레이터 스킬 참조):**
- 1게임 = 30분
- **남복/여복이 원칙, 혼복은 차선책** — 단 **여자 6명 이하면 혼복 1~2판 기본 보장**. 그 밖엔 ①남(여)자가 4명 미만이거나 ②같은 4명이 상대편까지 그대로 반복되거나 ③개인별 혼복희망을 적은 사람이 있을 때만. (혼복은 같은 팀 남자 구력 ≥ 여자 구력)
- **개인별 최소게임수 보장 (어기지 않는 규칙)** — 입력 '최소게임수' 칸에 적으면 min(값, 3연속 없이 가능한 최대 게임수) 이상 배정. 미달 시 review RETRY
- **남자 게스트는 혼복보다 남복 위주** — 혼복 남자 자리는 가급적 정회원(소프트). 여자 게스트는 혼복 가능
- **부부 페어** — '피함' 부부는 혼복 같은팀 회피, '원함'(한병익·전혜선)은 우대. 부부는 종료시각 30분 이내로 맞춤(소프트). 설정: 입력/클럽멤버_설정.xlsx 또는 내장 기본값. **예외: 신혁재·방미라는 반드시 정확히 30분 차이로 종료**(부부 시트 '종료시간차'=30분차이, 강한 소프트)
- **경기 사이 1시간 이상 공백 최우선 회피** — 기본 리듬 "30분 쉬고 다음 경기", 2슬롯 연속 정상. **3슬롯 연속은 금지(하드, 26.8.7~)** — 2게임 연속했으면 반드시 한 번 쉼. 못 채우면 코트 공석, review는 1건이라도 RETRY. 인원이 적으면 매치 수·공백이 늘 수 있음(의도된 트레이드오프). **개인별 예외 '연속게임' 칸(26.8.20~)**: 빈칸=기본 / `금지`=2연속도 금지 / `허용`=3연속까지 허용(4연속은 그대로 금지 — 짧은 시간에 많은 게임 보장이 필요할 때만). 자세한 내용은 CLAUDE.md 참조
- **대기시간 최소화 → 일찍 온 사람이 일찍 끝남** (개인 유휴시간 볼록 페널티로 분산)
- **코트 affinity**: A=여복/혼복 우선, B=남복 우선, C=무관
- **개인별 혼복희망** — 입력 '혼복희망' 칸에 적으면 그만큼 혼복 판을 열고 그 자리에 그 사람을 배정(남자 게스트도 본인이 적으면 '남복 위주' 면제). 아무도 안 적으면 기존 동작 그대로
- ~~여성 07:30 이전 슬롯 회피~~ **(26.8.14 폐지)** — 성별에 따른 시간대 제한 없음
- 같은 팀 페어 중복 회피(초선형 비용)
- **같은 4명 재대결 회피** — 단, 그 성별로 만들 4명 조합이 하나뿐이면(여자 정확히 4명) 편을 바꾼 재대결은 허용. 편 가르기 3가지가 6개 짝을 한 번씩 쓰므로 짝 중복 0으로 여복 3판까지 가능하고, 막으면 여복 1판+혼복 다수가 된다. 자세한 내용은 CLAUDE.md 참조
- 게임수 균형은 공평 목표치(물채우기) 기준
- 정회원/게스트 혼합 권장 (가중치 작음)
- **씨드 대진 (고정 배치, 26.8.20~)** — 입력 양식 **'씨드대진' 시트**(선택)에 대진 일부를 적어 두면 그 자리를 고정한 채 나머지만 채운다. 시트는 결과 대진표와 셀 좌표가 1:1(지난주 결과에서 복사·붙여넣기 가능). 한 칸=그 사람만 / 같은 편 두 칸=같은 팀 / 네 칸=경기 통째. 씨드는 연속게임·최대게임수 하드 규칙보다 우선하고(경고는 뜸), 물리적으로 불가능한 입력은 에러로 막는다. 비워 두면 기존과 완전히 동일. 자세한 내용은 CLAUDE.md 참조
- ⚠ 인원이 코트×4보다 훨씬 많으면 1시간 이상 공백을 0으로 만들 수 없다(최소화가 목표)
  - `long_gap_rate` RETRY는 코트 구조에서 나오는 산술적 결과이지 알고리즘 결함이 아니다 — 이것을 이유로 알고리즘·임계값을 손대지 않는다. 26.8.8~ 구조(96자리)에서는 매주 떴고, **26.9.5~ 구조(A·B 08~12시·C 07~09시 = 80자리)에서는 상시가 아니다**(22명 실측 27%로 PASS). 인원이 늘면 다시 뜬다. 코트 연장 불가 확정. 자세한 산술 근거는 CLAUDE.md 참조

**배포 원칙 (26.8.7~):** 사용자는 항상 웹페이지로만 사용 → **수정 후에는 반드시 커밋 → main 푸시로 웹 배포까지 완료**(deploy.yml이 web/→gh-pages 자동 배포). 배포 success·gh-pages 반영 확인까지가 작업의 끝. 상세는 CLAUDE.md 참조

**통합 실행 스크립트:**
- `대진표_생성.py` (CLI) — `python 대진표_생성.py --in <입력엑셀> [--out <출력>] [--date <날짜>] [--seed N]`
- `대진표_생성.bat` (윈도우 더블클릭/CLI) — 대화형/인자형 모두 지원 (시작 시 `C` 입력하면 입력 검사만)
- `--create-template [--prefill image]`로 빈/사전채움 입력 양식 생성
- `--check` — 입력 양식 검사만 (오류·경고·구성 요약, 생성 안 함). 테스트 샘플: `샘플/샘플_입력_테스트_21명.xlsx`

**파일 / 디렉토리 규칙:**
```
auto-tennis-bracket/
├── 대진표_생성.py / .bat   ← 실행 진입점
├── AGENTS.md
├── 입력/                    ← 회원이 채울 양식
│   └── 테니스_입력양식.xlsx
├── 출력/                    ← 생성된 대진표 (날짜별 자동 명명)
│   └── 테니스_대진표_<YYMMDD>.xlsx
├── 샘플/                    ← 참고 자료
│   ├── 샘플_입력_13명.xlsx
│   └── 화면 캡처 *.png
├── _workspace/              ← 알고리즘 중간 산출물 (01_parsed/02_bracket/03_review.json)
└── .Codex/                 ← 하네스 (agents/, skills/)
```

- `--in` 생략 시 자동으로 `입력/테니스_입력양식.xlsx` 사용
- `--out` 생략 시 자동으로 `출력/테니스_대진표_<날짜>.xlsx` (date 인자 또는 오늘)
- 사용자가 직접 만든 다른 양식이 있으면 `--in <경로>`로 명시 지정

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-05-29 | 초기 구성 (4-에이전트 팀 + 4-스킬) | 전체 | - |
| 2026-05-29 | 코트 affinity (A=여/혼, B=남, C=무관) + 여성 07:30 이전 회피 추가 | schedule.py, orchestrator | 사용자 요구 추가 |
| 2026-05-29 | 혼복 위반 hard rule → soft penalty 전환 | schedule.py, review.py | 풀에 여자 최고 구력 > 남자 최고 구력일 때 게임 못 들어가는 문제 해결 |
| 2026-05-29 | 한글 파일명(`테니스_입력양식.xlsx`/`테니스_대진표.xlsx`) 기본화 | 전체 | 사용자 요구 |
| 2026-05-29 | `build_template.py --prefill image` 옵션 추가 | tennis-input-template | 첨부 이미지 기반 일부 사전 채움 |
| 2026-05-29 | 통합 실행 스크립트 `대진표_생성.py` + `.bat` 추가 | 루트 | 사용자가 직접 실행할 수 있도록 |
| 2026-05-29 | `최대게임수` 컬럼 추가 (hard constraint) | 양식/parse/schedule/review/render | "이성돈 3게임만" 같은 개인별 제한 반영 |
| 2026-05-29 | 디렉토리 규칙(`입력/`, `출력/`, `샘플/`) + 출력 파일명 자동 생성(YYMMDD) | 루트 + 대진표_생성.py/.bat | 루트 폴더 정리 요구 |
| 2026-05-29 | 입력 양식 안내 시트에 7개 섹션으로 규칙 전체 정리 | build_template.py | 회원 공지용 자료 통합 |
| 2026-07-25 | 여복 품질 게이트 — 짝 중복·같은 4명 재대결·구력합 차이>4 중 하나라도 걸리면 혼복으로 전환. `good_singles_capacity()`로 혼복 상한 산정, `quad_repeat` 강화 | schedule.py, build_template.py, web/help.html | "구력합 안 맞는 여복은 재미없다, 그럴 바엔 혼복" 지적 |
| 2026-08-07 | 여자 정확히 4명일 때 여복이 1판에서 끊기던 문제 수정 — 4인 조합이 1개뿐이라 재대결이 구조적으로 강제되는데 정가를 물어 나머지가 전부 혼복이 됐다. 새 조합 유무를 보고(`fresh_quad_available`/`quad_forced_gender`) 감면(`quad_repeat_forced`), `clean_singles_available` 2-pass. 실측 여복 1→3, 혼복 8→4. 자세한 내용은 CLAUDE.md | schedule.py(+미러 2), build_template.py, web/help.html, SKILL.md | "여복이 1건밖에 안 나온다, 여복이 혼복보다 우선" 지적 |
| 2026-07-25 | 혼복 최소 보장 — 여자 6명 이하면 1~2판을 기본으로 넣음(`mixed_limits()`가 최소·상한을 함께 계산, 미달 시 강한 페널티). 교류전도 동일 규칙 | schedule.py, build_template.py, web/help.html | "여복만 돌면 늘 같은 사람끼리" 요구 |
| 2026-07-25 | 완전 중복 대진(같은 4명·같은 편) 회피 + `mixed_quota`(여자 4명이면 여복 3판이 한계 → 부족분만 혼복 1~2판) + 성별 게임수 쏠림 방지 + 여성 07:30 이전 배정 차단. 자세한 내용은 CLAUDE.md 참조 | schedule.py, review.py, web/*, build_template.py | "여자 게임수를 줄여 여복을 만든 것 아니냐" 지적 |
| 2026-07-25 | 혼복 억제(단일 클럽 페널티+여복 후보 확보) + 전역 목적함수 `full_score`(1시간+ 공백·대기·개인 유휴 볼록 페널티·물채우기 공평 게임수) + 2단계 탐색(그리디 초안 → 선수교환/경기이동 반복 로컬 탐색) + review 공백·대기 지표. 자세한 내용은 CLAUDE.md 참조 | schedule.py, review.py, 대진표_생성.py, web/* , build_template.py | "혼복 과다·1시간 이상 쉬는 텀·일찍 끝내기" 요구 |
| 2026-07-04 | 지난주/2주전 대진표 페어 회피(단일 클럽일 때만, 교류전 제외): 이전 결과 엑셀에서 이름 기준 페어 추출→소프트 페널티. CLI `--prev1/--prev2/--auto-prev/--prompt-prev`, .bat 대화형 자동추천, 웹 업로드칸. 자세한 06-28 교류전 변경 이력은 CLAUDE.md 참조 | history.py(신규), schedule.py, review.py, 대진표_생성.py/.bat, web/* | "1~2주전 대진표로 기존 페어 회피, 우리멤버끼리만" 요구 |
| 2026-08-01 | 개인별 최소게임수 보장(입력 컬럼 신설, min(값,가용슬롯) 이상 배정·미달 RETRY) + 남자 게스트 혼복 페널티(혼복 남자 자리 정회원 우선, 여자 게스트는 혼복 가능). 자세한 내용은 CLAUDE.md 참조 | build_template/parse/schedule/review/render, web/*, 스킬 문서 | "최소 게임수 보장 칸 + 게스트 단성복식 중심(남자 게스트만)" 요구 |
| 2026-08-02 | 부부 페어 지원: 멤버 설정 파일(멤버 25명 + 부부 시트) — 피함 부부 혼복 같은팀 회피/원함 우대, 부부 종료시각 30분 이내 맞추기. CLI --members(자동감지)·--create-members, 웹 멤버 설정 다운로드/업로드. 자세한 내용은 CLAUDE.md 참조 | parse/build_template/schedule/review, 대진표_생성.py, web/* | "부부 4쌍, 원하는 부부만 혼복 페어, 같이 끝나게" 요구 |
| 2026-08-03 | 운영시간 변경(26.8.8~) 사전채움 반영: 코트 기본값 A(1번)·B(2번) 07:00~12:00·C(3번) 07:00~09:00(슬롯 8→10), 원유철·이지은 08:00~11:00(본업 수업), Phase C 초안이 사전채움 메모 승계. 자세한 내용은 CLAUDE.md 참조 | build_template.py·build_draft.py(+미러), web/help.html, SKILL.md 2종 | 카톡 공지 "8/8부터 7~12시, 1·2번 코트 7~12시, 원유철·이지은 8~11시" |
| 2026-08-06 | 통계 시트 수식화(손 수정 시 자동 재계산): 숨김 시트 '통계_계산'의 사람×슬롯 매트릭스(출전→연속게임→휴식연속→직전공백→파트너)를 근거로 전 지표·전체 요약을 엑셀 수식으로, 빨간 강조는 조건부 서식으로. 혼복 판정은 팀 사이 칸 "혼" 표기 기준. 자세한 내용은 CLAUDE.md 참조 | render_bracket.py(+미러 2), web/help.html, tennis-excel-output SKILL.md(+미러) | "통계(최대 대기시간·연속 게임횟수 등)를 스크립트로 엑셀에 걸어달라" 요구 |
| 2026-08-06 | 정회원 '기본 반영 사항' 사전채움(경성현·이강진 ~11시, 김효순·박진우 ~10시, 남궁석 08:30~, 메모 표기) + 부부 시트 '종료시간차' 컬럼 신설 — 신혁재·방미라만 종료시각 반드시 정확히 30분 차이(couple_finish_exact 강한 소프트, review couple_gap30_missed). 자세한 내용은 CLAUDE.md 참조 | build_template/parse_input/schedule/review(+미러 2), web/app.js·help.html, 입력/ 재생성, SKILL.md 3종 | "정회원 기본 반영 사항 항상 사전채움 + 방미라·신혁재 종료 반드시 30분 차이" 요구 |
| 2026-08-06 | 게스트 기본 최대게임수 제약 제거 — build_draft '기본 최대4'는 요청된 적 없는 임의 기본값이라 삭제(--guest-max 기본 0=제약 없음, 명시 시에만 적용). 자세한 내용은 CLAUDE.md 참조 | build_draft.py(+미러), CLAUDE.md, SKILL.md 2종 | "게스트 최대게임수 제약한 적 없는데 왜 있나" 지적 |
| 2026-08-07 | 3연속 출전 하드 금지 — 그리디 예외 2개 폐지·Refiner 소프트 400→100000·review 1건=RETRY. 최소게임수 보장 한도·강제 투입 기준을 `max_games_no3`(3연속 금지 하 최대 게임수)로 교체해 '3연속 예외'를 '미리 태우기'로 대체. 실측(실전 20명 시드 4종): 3연속 2→0, 최소게임수·공백·게임수 분포 동일. 자세한 내용은 CLAUDE.md 참조 | schedule/review/parse_input(+미러 2), build_template(+미러 2), web/help.html, SKILL.md 3종(+미러), CLAUDE.md | "3연속하는 사람이 생긴다, 최소한 2번에 1번은 쉬게" 요구 |
| 2026-08-14 | 개인별 '혼복희망' 칸 신설 — 적은 사람이 있을 때만 혼복 판을 그만큼 열고 그 자리에 배정. 남자 게스트도 본인이 적으면 '남복 위주' 규칙 면제(이게 핵심). 비우면 기존 동작 그대로. 자세한 내용은 CLAUDE.md 참조 | parse_input/schedule/review/build_template(+미러 2), web/help.html | "이선우 혼복 3게임(가능하면)" — 부부페어가 아니라 본인이 혼복을 뛰고 싶다는 뜻 |
| 2026-08-14 | '여성 07:30 이전 슬롯 회피' 규칙 폐지 — 가중치 2종·상수·eff_in 성별 보정 삭제. 성별 시간대 제한 없음 | schedule/review/build_template(+미러 2), web/help.html, SKILL.md 2종 | "여자 7시30분 이후 제약 없애자" 지시 |
| 2026-08-14 | 혼복희망 결함 4건 수정 — ①클램프·경고 부재(오타 `30` 한 칸에 여복 3→1판, review는 PASS)로 `clamp_mixed_wish()` 공용화(CLI·웹 둘 다 호출) ②전역 좌석 보너스가 충족 여부를 안 봐 과충족 → 보너스 삭제 + `Refiner._wish_delta()` 신설(Refiner는 델타로만 판정해 전역 항을 못 본다) ③교류전 클럽 제약 미반영으로 필요 판수 과소계산 ④review 집계가 희망 충족분을 '남복 우선 실패'로 오도. 하한 인상은 1판으로 제한(폭주 레버 제거). 자세한 내용은 CLAUDE.md 참조 | parse_input/schedule/review(+미러 2), run.py, SKILL.md 4종, help.html, 샘플 2종 | Codex 적대적 리뷰 + code-reviewer 독립 리뷰 병렬, 검증은 오케스트레이터가 전부 재현 |
| 2026-08-14 | 혼복 하한 '1판 제한' 원복 — 게임수 균형 회귀(사용자 지적). 하한이 희망만큼이어야 초안 대부분이 희망 충족 상태가 되어 초안 선택이 균형을 보고 고른다. ablation으로 원인 단독 확정(분산 0.495→0.209, 기능 도입 전과 일치). 결함 4건 수정은 유지. 자세한 내용은 CLAUDE.md 참조 | schedule.py(+미러 2) | 사용자가 웹 산출물 2판 대조로 지적 |
| 2026-08-20 | **개인별 '연속게임' 칸 신설(빈칸/금지/허용) + 웹 최소게임수 경고 결함 수정**: 정반대 요구 두 건이 같은 축에서 나왔다 — 이성돈 "한 게임 뛰고 한 게임 쉬고 싶다"(2연속도 금지)와 백용승 게스트 `08:30~11:30`(6슬롯) **5게임 보장**(6슬롯에 5게임이면 쉬는 슬롯이 1개뿐이라 어떻게 배치해도 3연속이 강제된다 — 3연속 하드 금지의 개인 면제가 필수). 한 칸 3단계로 통합: 빈칸=현행(2연속 OK·3연속 하드 금지) / `금지`=`no2` / `허용`=`ok3`. ①`max_games_no3` → **`max_games_streak(slots, streak)`** 로 일반화(`no2`=인접 슬롯 동시선택 금지 DP, `ok3`=슬롯 수, 빈칸=현행 DP). 세 파일(schedule·review·parse_input)의 사본 모두 동일 일반화, `max_games_no3`는 호환 래퍼로 존치 ②그리디 하드필터 **`blocks_by_streak()`** 신설 — `enumerate_candidates` working_pool과 `_hard_filter` 양쪽 교체 ③전역 `player_timing_cost(..., streak)` + **`Refiner.timing()`에도 전달**(증분 delta가 모드를 못 보면 로컬 개선이 하드 규칙을 되살린다 — 과거 `_wish_delta` 사고와 같은 자리) ④가중치 신설 `W[consecutive_banned]=100000`·`W[three_consec_allowed]=20`·`G[two_streak_banned]=100000`·`G[three_streak_allowed]=40` (기존 값은 불변) ⑤review `two_consec_banned`(high, 1건이라도 RETRY)·`three_consec_allowed`(low, 위반 아님) 분리 집계 ⑥**덤으로 기존 결함 수정** — `web/py/run.py`가 최소게임수 경고를 CLI와 달리 **원시 슬롯 수**로 검사해(8/7 이후) 백용승 케이스에 웹만 침묵했다. 사용자는 웹만 쓰므로 사실상 무경고였다. CLI와 같은 `_max_games_streak` 기준으로 통일 ⑦**독립 리뷰(code-reviewer) 반영** — `허용`이 처음엔 **연속 길이 상한이 없어** 4·5·6연속까지 열렸다(안내문은 '3게임 연속까지'). 리뷰어 실측: `허용`만 켠 사람이 **게임수 이득 0인데 4연속**(`long_gap`·`idle_sq`가 '일찍 끝내기'를 보상하는데 소프트 40으로는 못 막음). → 세 모드를 **연속 허용 길이**로 환원(`STREAK_RUN_LIMIT={"":2,"no2":1,"ok3":3}`)하고 DP·하드필터·전역비용을 그 하나의 기준으로 통일: `is_four_streak()` 신설 → `허용`도 4연속부터 차단, 전역은 run 길이로 판정(4연속 이상 = `three_streak` 하드). 백용승 요구는 3연속 1회면 충족되므로 영향 없음(실측 확인). 함께 반영: 공백만 든 셀은 빈칸 취급, `ok3` 경고 라벨 단위 정정, 결과 엑셀 명단에 '연속 금지/3연속 허용' 표기, 주석 들여쓰기. DP 재검증: **1,536조합 × 3사본 불일치 0**(브루트포스 대조, 연속 6슬롯 → 기본 4·금지 3·허용 5). ⑧사전채움: 이성돈 `연속게임=금지` 상시화, **최종인·경성현 `09:00~12:00` 고정**(26.8.20~ 사용자 통보 — 경성현은 종전 07:00~11:00에서 변경) | parse_input/schedule/review/build_template/build_draft(+미러 2), web/py/run.py, 대진표_생성.py, web/help.html, SKILL.md 3종(+미러), CLAUDE.md/AGENTS.md | 이성돈 "연속게임 힘들다" + 백용승 "연속되더라도 5게임보장, 시간은 유지" 요구. **무변화 회귀**: 칸 미사용 시 `02_bracket.json` md5가 수정 전과 완전 일치 — 샘플21명 시드7 `92612b3d…`·시드42 `4bc988a9…`, 샘플13명 시드7 `b7e1cb17…` 3/3 통과. 구양식(8/15 입력, 컬럼 없음)도 정상 파싱. **기능 실측**(8/22 실전 22명, 시드 7/1/42): 이성돈 최장연속 **1/1/1**(09:30·10:30·11:30 식 격간), 백용승 **5/5/5게임**(3연속 1회, `three_consec_allowed`로 low 분류·RETRY 아님), 기본 선수 3연속 0·`금지` 위반 0·최소게임수 미달 0·최대게임수 초과 0, review PASS. 웹 경로(`run.py.generate_bracket`) 동일 결과 + `허용` 미지정 시 경고 정상 출력(수정 전엔 무경고) |
| 2026-08-20 | **씨드 대진(고정 배치) 신설 — 입력 양식 '씨드대진' 시트**: 사용자가 대진표 일부를 미리 정해 두면 그 자리를 고정한 채 나머지만 채운다. ①시트는 **결과 대진표와 셀 좌표 1:1**(헤더행 2·코트 base [3,8,13]·슬롯 성함행·컬럼 너비 실측 일치) → 지난주 결과에서 블록 복사·붙여넣기 가능. 한 칸=개인 고정 / 같은 편 두 칸=같은 팀 / 네 칸=경기 통째, 적은 칸 위치 그대로 ②`build_pin_index()`가 `(슬롯,코트)→pin`과 **`pin_slots`(예약)** 을 만들고, `streak_run_len()`이 배정∪예약을 **앞뒤 양방향**으로 훑어 연속 길이 판정(씨드 없으면 뒤쪽이 비어 종전 '뒤돌아보기'와 동일), `pins_ahead()`가 최대게임수 여유를 미리 깎음 — 예약을 안 보면 앞을 채운 뒤 뒤늦게 고정이 붙어 3연속·초과가 생긴다 ③`enumerate_candidates(require_ids=)`가 고정자를 슬라이스(top_k·SINGLES_TOP·MIXED_TOP) 절단 앞으로 올리고 그가 든 후보만 남김 + `_orient_for_pin()`(두 팀은 대칭이라 뒤집어 맞는 후보도 살림)·`_arrange_seats()`(적은 칸 그대로) ④**Refiner 좌석 잠금** — 고정 좌석은 `positions`에서 제외, 그 경기는 명단 통째 이동 금지, 나머지 자리는 계속 최적화 ⑤씨드는 하드 규칙 우선(하드필터·최대게임수 필터 면제), review가 `three_consec_seed`·`two_consec_banned_seed`·`max_games_seed`·`seed_not_kept`로 분리 집계(RETRY 아님) ⑥`parse_seed()` 검증 — 없는 이름·같은 슬롯 중복·미운영 코트·IN~OUT 밖·**만들 수 없는 성별 조합**은 에러, 남녀 혼재·최대게임수 초과·연속 한도 초과는 경고. CLI(`parse_input.main`)와 웹(`run.py`) 동일 ⑦결과 엑셀에 고정 이름 진한 파란색(`FONT_NAME_SEED`) 표시, 웹 결과에 "씨드 N/M자리 반영" 줄, `--check`에 씨드 요약 ⑧**독립 리뷰 2레인 반영**(Codex `high` 적대적 리뷰 + code-reviewer) — 초안에서 7건을 잡았다. (a) **같은 슬롯의 다른 코트 고정자를 앞 코트가 먼저 데려가** 정작 고정 자리가 공석이 되던 것(가용 인원이 딱 맞는 슬롯에서 재현) → `pin_court_of`로 그 코트 후보에서 제외 (b) 코트 헤더 중복 시 뒤 pin이 앞 pin을 덮어쓰는데 **예약은 둘 다 남아** 엉뚱한 사람이 묶이던 것 → 에러로 차단 (c) 그리드 경계가 없어 하단 메모 행('10:00 이후' 같은 글자)을 슬롯으로 오인 → 성함 행을 슬롯 수만큼만 읽고 같은 시각 중복은 에러 (d) 그 시간 가용 인원(남 n·여 m)으로는 **어떤 종류로도 완성 불가능한 고정**을 통과시키던 것 → 에러 (e) 교류전에서 같은 팀에 다른 클럽을 적어도 통과시키던 것(팀=같은 클럽은 하드 규칙) → 에러 (f) 씨드를 못 채웠는데 verdict가 **PASS**로 나오던 것 → RETRY (g) 씨드 면제 범위가 너무 넓어 **진짜 알고리즘 버그를 가리던** 것 → 최대게임수는 '씨드 자리 수까지'만 면제, 연속 위반은 그 구간이 **전부** 씨드일 때만 면제(하나만 걸쳐도 면제하면 피할 수 있었던 위반이 묻힌다). **반려 1건**: '팀 안에서 두 사람의 칸 위치가 바뀌면 미유지로 봐야 한다'는 지적은 복식에서 같은 팀 안의 자리 순서는 의미가 없어 반려. 7건 전부 재현 테스트로 확인(같은슬롯 고정 유지·중복헤더 차단·메모행 무시·인원부족 차단·클럽위반 차단·면제범위 4케이스). ⑨**2차 독립 리뷰(code-reviewer) 반영** — HIGH 3건이 더 나왔고 전부 재현 후 수정. (h) **씨드 입력 오류가 전부 무음이었다.** 한 칸 어긋나게 붙여넣으면 이름이 VS칸으로 밀려 그 사람은 소멸하고 나머지는 옆자리로 밀려 **다른 대진이 확정**되는데, 웹은 '적어 두신 자리가 모두 그대로 들어갔습니다'라며 **틀린 씨드를 성공으로 확인**해 줬다. '결과' 행에 붙여넣거나 시간 라벨이 지워지면 씨드가 통째로 증발하면서도 무고지. → VS칸 침범 검사 + **격자 안에서 읽지 않은 자리에 참가자 이름이 있으면 에러**(두 경우를 함께 잡는다). (i) **슬롯 단위 자리 경합이 모델링돼 있지 않았다.** 완성 가능성 검사가 pin 하나씩 독립적으로만 봐서, 같은 슬롯의 다른 코트가 같은 인원을 먼저 쓰는 것을 놓쳤다(여자 5명 슬롯에 A코트 여복4 + B코트 혼복 → 여자 6명 필요인데 통과). → 슬롯별 '필요 최소 남/여 합계 ≤ 가용 인원' 검사 추가(종류 미정 고정은 가장 적게 쓰는 종류로 세어 확실한 하한). (j) **고정 있는 코트를 나중에 채워** 그 경기를 완성할 '이름을 안 적은 자리'의 후보를 앞 코트가 데려가던 것 — `pin_court_of`는 이름이 적힌 사람만 뺐다. 리뷰어 실측 **초안 39/40 공석**(탐색이 40→1로 붕괴). → 슬롯 안에서 **고정 있는 코트를 먼저** 처리(`sorted(..., key=... not in pin_map)`, 씨드 없으면 안정 정렬이라 순서 불변). 재측정 **유실 0/10**. 함께 반영: `full_score`에 `seed_missing=20000/자리` 신설(초안 선택이 씨드를 지킨 안을 고르게 — 종전엔 우연히 상쇄되고 있었다), 웹 결과에 **씨드 목록**(시각·코트·팀별 이름, 빈자리는 ＿) 노출(CLI `--check`에만 있던 확인 수단), `--check` 목록도 빈 자리를 접지 않고 자리표시자로, 씨드 격자가 '코트 기본값 기준'임을 시트 1행에 명시, `_arrange_seats` 중복 id 방어, `seed_not_kept` 메시지·주석 정정. **Positive**: 리뷰어가 무씨드 md5 4/4 일치·Refiner 좌석 잠금 이탈 0건(부분 고정×시드 8개×kicks 60)·미러 10종 md5 일치를 독립 확인. | schedule/review/parse_input/build_template/render_bracket(+미러 2), web/{py/run.py,app.js,help.html}, 대진표_생성.py, SKILL.md 4종(+미러), CLAUDE.md/AGENTS.md | "입력양식 안에 별도 시트로 현재와 같은 대진표를 만들어주면 내가 입력한다" 요구. **무변화 회귀**: 씨드 미사용 시 `02_bracket.json` md5가 수정 전(HEAD)과 완전 일치 — 샘플21명 시드7 `92612b3d…`·시드42 `4bc988a9…`, 샘플13명 시드7 `b7e1cb17…` 3/3, review verdict도 동일. **기능 실측**(25명 사전채움, 시드 7): 9자리 고정(1자리·4자리 통째·같은팀 2자리·혼복 강제) 전부 유지, 혼복 강제 자리 type=X·남복 통째 고정 type=M, 3연속 0·최소게임수 미달 0·최대게임수 초과 0. Refiner 스트레스(kicks=200)에도 전 자리 유지, 웹 경로(`run.generate_bracket`) 동일. 대가: 1시간+ 공백 13→15건·총 대기 2310→2400분·게임수 격차 2→3·팀 구력차 0.71→1.04 |
| 2026-09-03 | 코트 운영시간 변경(26.9.5~): 1·2번(A·B) 코트 07:00→**08:00 시작**, 3번(C)은 07:00~09:00 유지 → 전체 자리 96→80개, 1인 평균 4.4→3.6게임. 사전채움 개인 IN시간은 07:00 그대로. 안내시트·씨드격자·help.html·SKILL.md 문구 동반 갱신. 자세한 내용은 CLAUDE.md 참조 | build_template.py(+미러 2), web/help.html, SKILL.md 2종(+미러) | "이번주부터 A,B코트가 8시부터 시작" 통보. 실측 22명 시드7: PASS, 3연속 0, 공백 27% |
